---
layout: layout.njk
---

<ul>
    {%- for page in collections.pages -%}
        {% if page.data.title and page.url %}
            <li>
                <a href="{{ page.url }}">{{ page.data.title }}</a>
            </li>
        {% endif %}
    {%- endfor -%}
</ul>
